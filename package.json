{
  "name": "star",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p build && browserify index.js -d -s Star -o build/star.js",
    "watch": "mkdir -p build && watchify index.js -d -s Star -o build/star.js",
    "postinstall": "mkdir -p build && browserify index.js -d -s Star -o build/star.js",
    "start": "node server"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gl-matrix-mat3": "^2.2.1-npm",
    "gl-matrix-vec2": "^2.2.1-npm",
    "lodash": "^2.4.1",
    "noisejs": "^2.1.0",
    "preach": "^0.2.2",
    "watchify": "^2.1.1"
  },
  "dependencies": {
    "browserify": "^6.3.3",
    "express": "^4.10.4",
    "performance-now": "^0.2.0",
    "present": "0.0.6",
    "raf": "^2.0.4",
    "ws": "^0.6.2"
  }
}
